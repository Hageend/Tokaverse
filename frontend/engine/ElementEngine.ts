// engine/ElementEngine.ts
// Motor de elementos financieros — cálculo de multiplicadores y efectividad

import { Element, ElementEngine as ElementEngineBase, CLASS_ELEMENTS, BOSS_ELEMENTS, ELEMENT_INFO } from '../types/elements'

export { Element, CLASS_ELEMENTS, BOSS_ELEMENTS, ELEMENT_INFO }
export { ElementEngine } from '../types/elements'
